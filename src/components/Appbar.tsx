import { AppBar, Toolbar, Typography } from '@mui/material';

function Appbar() {
    return (
        <AppBar className="">
            <Toolbar>
                <Typography>
Soofa
                </Typography>
            </Toolbar>
        </AppBar>
    )
}

export default Appbar
